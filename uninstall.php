<?php
/**
 * This file runs when the plugin in uninstalled (deleted).
 *
 * @package MB Only Posts in Search/Uninstall
 */

// if uninstall.php is not called by WordPress, die
if (!defined('WP_UNINSTALL_PLUGIN')) {
    die;
}